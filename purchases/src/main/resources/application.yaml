spring:
  application:
    name: purchases
  rabbitmq:
    host: ${RABBIT_HOST}

db:
  name: eclub
  username: eclub
  password: ${DB_PASSWORD}
  host: ${DB_HOST}

stock-queue:
  name: stock.transactions
  routing-key: stock.transactions